[workspace]
members = ["adx"]

[profile.release]
codegen-units = 1
lto = "thin"
panic = "abort"

# generated by 'cargo dist init'
[profile.dist]
inherits = "release"
debug = true
split-debuginfo = "packed"
